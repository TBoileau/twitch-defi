{% extends 'base.html.twig' %}

{% block body %}
    {{ form_start(form) }}
        {{ form_errors(form) }}
        {{ form_row(form.name) }}
        {{ form_row(form.description) }}
        <div>
            <button type="submit" class="btn btn-block">Soumettre</button>
        </div>
    {{ form_end(form) }}
{% endblock %}
